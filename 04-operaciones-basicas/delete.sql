/*Borrar registros*/
DELETE FROM usuarios WHERE id = 4;
/*Sentencia DELETE FROM + nombretabla + WHERE (para indicar exactamente que borrar y no eliminar toooodo) + cosa que queremos borrar

Borrar con id es más seguro, pues sabes que solo se eliminará un elemento.*/